# 学習メンター 運用ガイド（運用担当者向け）

## これは何か

AIに「学習と解説だけをする」役割を与えるための設定です。コードを書かせず、解説・レビュー・トラブル対応だけをさせます。

**目的は2つ。**

1. **メンター負担の一次対応を引き受けること** — 「まずAIに聞く、詰まったら人に聞く」の"AI"の部分
2. **理解しないままコードが増えるのを防ぐこと** — レビュー機能に「自分のコードを説明できるか」のゲートを組み込んでいます

**AIを使わせない設定ではありません。** 使い方を、理解が残る側に寄せるための設定です。

## 配布物

| ファイル | 中身 |
|---|---|
| `learning-mentor-prompt.md` | 役割定義そのもの。**唯一のソース。** 更新するときはここだけ直す |
| `learning-mentor-setup.md` | 環境ごとの配置をAIにやらせるためのプロンプト |
| `learning-mentor-usage.md` | **使い方。** 解説・レビュー・トラブル対応の3モードと、追加でできること（GitHub MCP・運用A / B・モデル）。**README は導入までを担い、使い方はこちらが持つ。** まだ配れない選択肢は [docs/tuning-options.md](https://github.com/winc1980/learning-mentor/blob/main/docs/tuning-options.md) に置く（リポジトリ内の文書なので zip には入らない） |
| `learning-mentor-ops-guide.md` | この文書（運営側向け） |
| `check-sync.ts` | **本文のコピーがずれていないか確認するスクリプト。** 本体を更新したら実行する（Bun） |
| `.claude/agents/learn.md` | **参照実装。** 本体プロンプトに frontmatter を付けただけのもの。`claude --agent learn` で起動して動作を確かめる用。**本文は `learning-mentor-prompt.md` のコピーなので、本体を更新したら作り直す** |
| `mentor-update.ts` | **導入と更新の両方を行うスクリプト（Bun）。** `--setup` で受領書の記録・hook の設置・配置先の `.gitignore` 追記・動作確認の案内、`--check` で状態表示、`--apply` で貼り直し |
| `hooks/` | セッション開始時に新版を確認する設定の雛形と、**手で入れる場合の**置き方（通常は `--setup` が入れる） |
| `VERSION` | 配布物のバージョン。タグと一致していないとリリースが止まる |

---

## 2つの運用パターン

### 【A】常時適用型 — 本当の初学者向け

**対象：** プログラミングを始めたばかりの人、Web学習会の参加者

**運用：**

- **1人1つ、学習用リポジトリを作る**
- そのリポジトリ直下に本体プロンプトを配置（`CLAUDE.md` / `AGENTS.md` 等）
- **そのリポジトリではAI駆動開発を禁止**。コードは自分で書く
- 加えて、ツール側で **編集権限を落とす設定**を必ず併用する（指示文だけでは長い会話で効かなくなります）
- **編集系ツールだけを塞いでも穴が残ります。** シェル実行が使えれば `cat > file` でファイルは書けます。拒否リストではなく、読み取り専用ツールの**許可リスト**にするのが確実です

**なぜリポジトリを分けるか：** 開発用リポジトリの直下に置くと、そのリポジトリでの作業が全部「コードを書かない」になってしまいます。学習用は最初から別にします。

**導入実績（2026-09-08）。** WINC の参加者2名が、**Codex（CLI／デスクトップアプリ）で【A】の配置に成功**し、
メンターとして会話できました。**配布物の手順で、Claude Code 以外でも導入が通ることが初めて確認できた事例です。**

ただし確認できたのはそこまでです。**参加者の環境では、次の2つは未確認のまま**なので、
「Codex は検証済み」と読まないでください。

- **読み取り専用が本物かどうか**（下の Codex 用の確認手順を通していない）
- **更新のお知らせ（hook）が発火したかどうか**（[#16](https://github.com/winc1980/learning-mentor/issues/16)）

**読み取り専用の確認だけは、導入時に必ず一緒に通させてください。** ここが【A】の要で、
指示文だけでは長い会話で効かなくなるからです。

### 【B】呼び出し型 — 経験者・実プロジェクト向け

**対象：** 開発プロジェクトに入っている人

**運用：**

- 各プロジェクトに `learn` として登録し、必要なときだけ呼ぶ
- 普段は通常の開発セッション。分からないことに当たったときだけ切り替える
- **メンターを「セッション本体」として起動する。** 定義ファイルを置くだけでなく、起動方法まで揃えて初めて機能します（Claude Code なら `claude --agent learn`）

**ここが一番間違えやすい箇所です。** 詳しくは配置用プロンプトの冒頭に書きましたが、要点だけ：

| やり方 | 結果 |
|---|---|
| スラッシュコマンドで開発セッションに指示を注入 | **編集ツールを持ったまま**なので、長い会話では能力のほうが勝つ |
| 読み取り専用サブエージェントを開発セッションから呼ぶ | **本人が直接会話できない。** サブエージェントは最終レポートを1本返して終わる委譲なので、はしごも説明ゲートも動かない。しかも編集ツールを持った親が「代わりに実装しましょうか」と申し出てくる |
| **同じ定義をセッション本体として起動** | 編集ツールなし＋直接対話。**これが正解** |

**サブエージェントの定義ファイル自体は正しくても、呼び出し方を間違えると失敗します。** 「コードを書かない」はサブエージェントへの制約であって、それを呼び出す親セッションへの制約ではないからです。

**判断基準：** フォルダを移動しないと使えない形にすると、まず使われません。**呼び出しコストを限りなくゼロに近づけてください。**

**コードの貼り付けも呼び出しコストです。** GitHub MCP を**読み取り専用で**繋いでおくと、「この PR 見てもらえますか」だけでレビューに入れます。手順は配置用プロンプトの「補助：GitHub MCP を読み取り専用で繋ぐ」を参照してください。**必ず読み取り専用で繋いでください**（理由は下記）。

---

## 3つの機能

| 機能 | 何をするか |
|---|---|
| **解説** | 何が分かっていないかを特定 → 前提確認 → 全体像 → 詳細 → **最後に本人に説明させる** |
| **レビュー** | **説明ゲート**（自分の言葉で説明できるか）→ 重大度付きの指摘。**修正コードは書かせない** |
| **トラブル対応** | 5点セット（やりたいこと / 起きたこと / エラー全文 / 試したこと / **自分の仮説**）を揃える → 仮説を検証する実験を一緒に設計 → 実行は本人 |

トラブル対応で最初に揃える5点は、**人に質問するときのテンプレとまったく同じ**にしてあります。AIに聞く練習が、そのまま人に聞くときの質問の質になります。

---

## 学習会・プロジェクトでの使い方

### 質問の一次窓口として

運用ルールは一行だけ。

> **まずAIに聞く。詰まったら人に聞く。**

人に聞くときは、トラブル対応の5点セットを添える。これで、経験者に届く質問の質が上がります。

### マージ前のセルフチェックとして

レビュー機能を、人間レビューに出す前に自分で通す。説明ゲートで詰まったら、そこは自分がまだ理解していない箇所です。

**人間レビューの負担が減るのは、指摘が減るからではなく、「説明できない箇所」を持ち込まれなくなるからです。**

---

## 効かなくなるパターンと、その対処

正直に書いておきます。**これはルールではなく道具なので、抜け道はあります。**

| パターン | 対処 |
|---|---|
| 別のセッション／別のツールで完成コードを取ってきて貼る | **設定では防げません。** 担保は人間側の「説明できること」ゲートのほうです。この設定はそれを楽にする道具であって、代わりにはなりません |
| 会話が長くなると指示が効かなくなる | 指示文で禁止せず、**ツールを持たせない**。【A】【B】とも、編集系ツールを外した状態で起動します |
| **配置したのに、メンターではなく開発セッションが応答する** | 呼び出し方の誤り。定義ファイルを置いただけでは、通常の開発セッションから呼ばれる部品にしかなりません。**セッション本体として起動できているか**を確認してください（下の動作確認を参照） |
| **MCP サーバーを繋いだら書けるようになってしまった** | **MCP の追加は「能力の追加」です。** GitHub MCP には PR 作成・マージ・ファイル push が含まれるため、素で繋ぐと「ローカルは編集できないのにリモートには書ける」状態になります。①読み取り専用 PAT ②サーバー側の読み取り専用モード ③ツール許可リスト、の3層で防ぎます |
| 「もういいから答えを教えて」に押し切られる | 本体プロンプト側にヒントを3段階で具体化する手順を入れてあります。3段目でも詰まる場合は前提知識の解説に切り替わります |
| 詰まり続けて離脱する | 「同じところで3往復したら実装の話を止めて前提の解説に切り替える」を入れてあります。詰まらせ続けるのが目的ではありません |
| 面倒で使われない | **これが一番起きます。** 呼び出しコストを下げること（【B】をコマンド化する理由）と、学習会の中で1回実際に使わせることの2つで対処してください |
| **説明を求められるのがしんどくて使わなくなる** | **2番目に起きます。** 本体プロンプトの「説明を求めるときの原則」で対処しています（下記） |

### 説明ゲートの負荷について

「自分の言葉で説明させる」は効果が大きい代わりに、**負荷も大きい**手法です。しかも **理解が曖昧なときほど難しく、そのときほど効果が高い。** 一番効かせたい場面が一番しんどい場面になるので、そこを放置すると離脱します。

本体プロンプトには、負荷を下げるための仕掛けを入れてあります。

- **入り口を「全部説明して」ではなく「一番自信がない箇所はどこ？」にする** — 負荷が低く、診断としてはむしろ強い。自分で選ぶので詰問感がない
- **4段階のはしご** — 全体を説明 → 1箇所に絞って説明 → 予測 → 選択肢。詰まったら降り、答えられたら上げる
- **予測課題（「この行を消したらどうなる？」）が既定** — 説明より負荷がずっと軽いのに、診断力は落ちない
- **最下段は選択肢** — 必ず答えられる段を用意しておく。当たったら予測へ戻すので、まぐれはそこで露見する
- **不完全な答えは、合っている部分を先に拾ってから続きを聞く** — 訂正から入らない
- **採点の言葉を禁止** — 「正解」「違います」「理解度〇%」を使わせない
- **「分からない」を正式な回答として扱う**
- **逃げ道を常に開ける** — 「いま説明したくない」と言われたら即座に引く
- **代替形式を許可** — 箇条書き、コードへのコメント書き足し、矢印だけの図でもよい

**目指しているのは、完璧な説明をさせることではなく、不完全でも言葉にしてみる習慣です。** 「たぶん〜」「なんか〜」から始まる説明が出てくれば成功で、そこから会話で詰めていけます。

---

## 配置後の動作確認（必ずやってください）

**配置しただけでは効いているか分かりません。** 間違った場所に置いた場合、エラーは出ず、ただ黙って無視されます。次の2つを毎回確認してください。

### 1. メンターとして応答するか

「このコード見てもらえますか」＋短いコードを渡します。**いきなりレビューが返ってきたら失敗です。** 正しく効いていれば、まず「自分でも一番自信がない箇所はどこですか」と聞き返してきます。

### 2. 本当に編集できないか ── こちらが重要

**必ず「ツール一覧」で判定してください。断り方では判定できません。**

> 使えるツールの名前を、箇条書きで全部挙げてください。

| 返ってきた一覧 | 判定 |
|---|---|
| 読み取り系だけ（`Read` `Grep` `Glob` など） | ✅ 正しい |
| `Edit` `Write` **またはシェル実行ツール**が含まれる | ❌ 失敗。シェルがあれば `cat > file` で書けます |

**なぜ断り方で判定できないのか。** 「このファイルを書き換えて」と頼むと、ツールが無い場合でも次のように返ってきます。

> このセッションではファイルの作成・変更はできません。書き込みは行わない**設定になっています**

これは一見「指示文を根拠に断っている（＝能力は残っている）」ように読めますが、**実際にはツールが無いときの応答です。** ツールが無いモデルは「呼び出しに失敗する」のではなく、**そもそも選択肢が無いまま自然言語で説明する**だけなので、方針による自制と区別がつきません。

**能力の有無は、応答の言葉づかいには現れません。ツール一覧にしか現れません。**

#### Codex CLI だけは、この方法で判定できません（2026-09-08 実機確認）

**Codex は読み取り専用に設定しても、ツール一覧からシェル実行が消えません。** 上の表のとおりに読むと、正しく配置できているのに「失敗」と判定してしまいます。実際、同じ設定で2回聞いたら、返ってくるツール一覧の中身自体が違いました。**自己申告が当てにならないのは、断り方だけでなくツール一覧も同じです。**

Codex では代わりに、**サンドボックスが実際に書き込みを拒むかどうか**を見てください。モデルを呼ばないので、無料で何度でも試せます。

```bash
# 1. 拒否されれば合格（probe.txt ができない）
codex sandbox -P ":read-only" -C <確認したいディレクトリ> -- powershell -NoProfile -Command "Set-Content -Path probe.txt -Value x"

# 2. 対照：こちらではファイルができる
codex sandbox -P ":danger-full-access" -C <同じディレクトリ> -- powershell -NoProfile -Command "Set-Content -Path probe2.txt -Value x"
```

**必ず2つとも実行してください。** 1だけだと、拒否されたのが権限のせいなのか、コマンドを打ち間違えただけなのか区別がつきません。2でファイルができて初めて、1の失敗が権限によるものだと言えます。終わったらできたファイルは消してください。

### 3. MCP を繋いだ場合、書き込み系が本当に無いか

ここも**ツール一覧で判定します。** 上と同じ質問をして、次の2点を見てください。

- `mcp__github-ro__` で始まるツールに、**書き込み系（`create_` `update_` `merge_` `push_` `delete_` `_write`）が1つも無い**こと
- **フルアクセス版のサーバー（`mcp__github__` で始まるもの）が1つも混ざっていない**こと

「この Issue にコメントして」と頼んで断られたことは、**確認になりません**（理由は上と同じ）。

### 4. 代わりに実装を申し出てこないか

分からない箇所を1つ相談して、会話を3〜4往復させます。**「代わりに実装しましょうか？」が出たら、メンターではなく開発セッションが応答しています。** 呼び出し方が間違っている合図です。

---

## 作業の管理

**これからやることは GitHub Issues に置きます。** リポジトリ内に TODO ファイルは置きません
（`TODO.md` と `experiments/BACKLOG.md` は 2026-09-08 に廃止。中身は issue へ移しました）。

| 置き場所 | 何を置くか |
|---|---|
| **Issues** | これからやること。着手できる単位に分ける |
| **Milestones** | 期日のある段階（学習会・phase3 など）。**残作業を持つのはここと Issues だけ** |
| **コミットメッセージ** | 完了したことと、その理由。**進捗記録をファイルに残さない** |
| **`experiments/DECISIONS.md`** | **測らないと決めたこと。** issue にすると閉じて忘れられるため |
| **`experiments/README.md`** | 実験の手順と到達点 |
| **`docs/concept.md`** | 企画の意図・やらないこと・成功の判定。**腐らないものだけ** |
| **`docs/tuning-options.md`** | まだ配れない選択肢と、その根拠の強さ。**配れる状態になったら `learning-mentor-usage.md` へ移す** |
| **`CLAUDE.md`** | リポジトリを編集する人・AI 向けの規約 |

ラベルは5系統。`experiment`（ハーネスでセルを回す測定実験）/ `evaluation`（実学習者での評価）/
`delivery`（配布・配置・更新）/ `harness`（実験基盤）/ `feedback`（利用者から届いた声）に、
`priority:high` `priority:medium` `priority:low`、着手できないものに `blocked` を付けます。

**`experiment` と `evaluation` を混ぜないでください。** 前者は固定台本・AI 被験体で
規定の遵守を測るもの、後者は実学習者を観察するもので、言えることの範囲がまったく違います。

**利用者の声は Google フォームで受けます。** 参加者に GitHub を開かせないためです。
フォームは[こちら](https://docs.google.com/forms/d/e/1FAIpQLSeltEAaD7Q86DgPaqVQnhO84CIWZQl-eblp9zJ6hHeV5m66Lg/viewform)（README の「使ってみて気づいたことを送る」からも辿れます）。
運営がフォームの回答を `feedback` の型に写して起票します。**そのとき、報告者が
配置の動作確認を通していたかを必ず確認してください。** 通していない報告は、
「メンターの挙動が変」と「そもそも普通のAIと話していた」を区別できません。

issue を立てるときは `.github/ISSUE_TEMPLATE/` の型を使ってください。
**「やること一行」の issue を作らないための枠**です。とくに実験は
**判定（どういう結果が出たら何を結論するか）を回す前に書く**こと。
あとから解釈すると、出てきた数字をどちらの方向にも読めてしまいます。

---

## 導入の順番（提案）

1. **まず自分で1週間使う。** 実際に解説とトラブル対応をさせて、噛み合わない箇所を本体プロンプトに反映する
2. **学習会で1回、その場で使わせる。** 配って終わりだと使われません。1回一緒にやると定着します
3. **プロジェクトに `learn` を入れる。** 経験者側は、レビュー前のセルフチェックとしての使い道を先に見せる。**配置したら必ず動作確認まで行う**（下記）
4. **本体プロンプトの更新は1箇所で。** 各自の環境に配置されたコピーは古くなります。更新の届け方は下の「更新を届けるとき」を参照してください
5. **hook は配置手順に組み込み済み。** 後から入れてもらうのは、まず実行されません。`mentor-update.ts --setup` が配置直後に設置するので、**その実行まで見届けてください**（`--tool` を間違えると別の場所に書かれます）。**2つ目以降のリポジトリに入れる人も手作業は要りません**（v0.1.2 以降。既存の登録は最新の1本にまとめられ、配置先の記録は足されます）
6. **チームの共有リポジトリに【B】で配置させるときは、`--setup` まで通させる。** 配置先は人によって違うので、`.gitignore` への追記がないと「AIメンター導入」だけのPRが参加者の数だけ飛んできます。`--setup` を実行すれば `agent` 種別の配置先は自動で `.gitignore` に入ります。`CLAUDE.md` などに他の記述と同居させた（`embedded`）場合は自動で除外できないので、実行結果の★を見て個別に対応してください

---

## 更新するとき

本体プロンプトだけを直してください。配置用プロンプトと運用ガイドは、本体を貼り付ける器なので、原則変更不要です。

**ただし本文のコピーが2箇所あります。** `learning-mentor-setup.md` の埋め込みと、`.claude/agents/learn.md` の参照実装です。本体を直したら、この2つも貼り直してください。

貼り直したあと、必ず次を実行して確認します。

```bash
bun check-sync.ts
```

```
OK   learning-mentor-setup.md （279 行）
OK   .claude/agents/learn.md （279 行）

すべて一致しています。
```

ずれている場合は、どの行が違うかを差分で表示します。**ずれても何のエラーも出ないまま配布されるのが一番まずい**ので、更新のたびに通してください。

**ただし1箇所だけ例外があります。** 配置用プロンプト末尾の「補足：環境ごとの目安」は、各ツールの実際の機能名・パス・起動コマンドを載せているため、**放置すると確実に古くなります。** この種の機能は改称・非推奨・削除が頻繁に起きます（実際、Codex のカスタムプロンプトは非推奨になり、Copilot のチャットモードは「カスタムエージェント」に改称されました）。

半年に一度くらい、**AIに公式ドキュメントで裏を取らせて**更新してください。表には調査時点を明記してあります。

---

## 更新を届けるとき

本体を直して `check-sync.ts` が通ったら、次の順で配ります。

1. `VERSION` を上げる（例 `0.1.0` → `0.2.0`）
2. コミットして、同じ番号のタグを打って push する

```bash
git tag v0.2.0 && git push origin v0.2.0
```

あとは GitHub Actions が、配布物を固めた zip を添えて Release を作ります。
途中に関門が2つあり、どちらかに引っかかると **リリースは作られません。**

| 関門 | 止まる理由 |
|---|---|
| タグと `VERSION` の一致 | ずれると「v0.2.0 と名乗る v0.1.0 の本文」が出回る |
| `check-sync.ts` | コピーがずれたまま配ると、受け取った側は何のエラーも見ずに別物を使う |

### 受け取る側で何が起きるか

hook を入れている人は、次にセッションを開いたときに新版が出たことを知らされます。
**書き換えは自動では行われません。** 各自が次を実行します。

```
bun mentor-update.ts --apply
```

差分の要約を見て `y` と答えると貼り直され、元のファイルは `.bak` で残ります。

**なぜ自動にしないか。** メンターは読み取り専用のセッションなので、そもそも自分を
書き換えられません。そして無断で書き換えると、学習者に知らせないままメンターの挙動が
変わります。気づくのは自動、書き換えるのは人、という切り分けにしてあります。

### 更新したら、動作確認をもう一度

貼り直しは本文を丸ごと入れ替えます。上の「配置後の動作確認」を、**更新のたびに**
通してください。特にツール一覧の確認は省かないでください。

### hook が効いているかを確かめる

hook は、動いていなくてもエラーを出しません。**Codex では発火しないという報告があります**
（[openai/codex#17532](https://github.com/openai/codex/issues/17532)、2026-09-07 時点で open）。
効いているかどうかは次で判定します。

```
bun mentor-update.ts --check
```

`hook :` の行が `★ 一度も動いていません` なら、設定が効いていません。詳しくは `hooks/README.md`。

**Codex では hook を主たる経路にしないでください。** 学習会の節目に `--check` を手で
回すことを、正規の手順として案内してください。
