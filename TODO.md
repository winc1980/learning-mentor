# TODO（プロジェクト全体）

実験の設計・進捗は [experiments/BACKLOG.md](experiments/BACKLOG.md) を参照。
ここには実験（測定）に分類されない、配置・配布・リリースまわりの作業を置く。

最終更新 2026-09-08（v0.1.0 リリース作業）

---

## 未完了

### Codex CLI での配置が実際に成功するかの検証

**現状。** `learning-mentor-setup.md` は Codex CLI 向けに具体的な構成を指定しているが、
**一度も実機で試していない。** 机上で書いた手順がそのまま配布されている。

| 指定している項目 | 検証状況 |
|---|---|
| プロファイル `~/.codex/learn.config.toml` | **文献確認済み（2026-09-08、下記）。実機配置は未検証** |
| `model_instructions_file` で組み込み指示を置換 | **文献確認済み（2026-09-08）。実機動作は未検証** |
| `sandbox_mode = "read-only"` **または** `default_permissions = ":read-only"` | **文献確認済み（2026-09-08）。実機動作は未検証** |
| 起動コマンド `codex --profile learn` | **文献確認済み（2026-09-08）。実機動作は未検証** |

**なぜ重要か。** 運用ガイドが繰り返し警告しているとおり、
**配置に失敗しても エラーは出ず、黙って無視される。** 学習者は「メンターを使っている」
つもりで通常の開発セッションと話し続けることになり、しかもそのセッションは
**編集ツールを持っている。** 「コードを書かせない」という設計目的が丸ごと崩れる。

さらに Codex は変化が速い。`~/.codex/prompts/` のカスタムプロンプトは既に非推奨（Skills へ移行）
になっており、**プロファイルと `model_instructions_file` も同じ経路をたどっている可能性がある。**
運用ガイド L206 が「この表は放置すると確実に古くなる」と名指しで警告しているのが、まさにここ。

**やること。** 測定実験ではなく配置検証。WINC メンバーと同じ手順を、こちらで一度なぞる。

1. Codex CLI を素の状態で入れ、`learning-mentor-setup.md` の記述**だけ**を見て配置する
   （記憶や推測で補わない。補わないと進めない箇所が、そのまま手順の穴）
2. **公式ドキュメントで各キーの現行性を確認する。** `model_instructions_file` が
   まだ存在するか、`sandbox_mode` と `default_permissions` のどちらが現行か
3. 運用ガイド L134-172 の動作確認4項目を Codex で実施する
   - **ツール一覧を出させる**（断り方では判定しない。ops-guide L144）
   - 短いコードを渡して、いきなりレビューが返らず説明ゲートが来るか
   - 3〜4往復させて「代わりに実装しましょうか」が出ないか
   - MCP を繋いだ場合、書き込み系ツールが1つも無いか
4. 詰まった箇所と、ドキュメントとの食い違いを記録する

**ここが Claude Code と決定的に違う点。** Claude Code は `--output-format stream-json` の
init イベントに**ハーネスが実際に渡したツール配列**が入るため、機械的に検証できた
（実験では全204ターンでこれを照合している）。**Codex に同等の機械可読な検証手段があるかは不明。**
無ければ、ツール一覧を自己申告させる手動確認に頼るしかなく、
運用ガイドが「自己申告では判定できない」と書いている状態に戻る。**まずここを調べる。**

**規模。** 実機作業 1〜2時間。API コストはほぼゼロ。

**判定と分岐。**

- **配置できて読み取り専用も確認できた** → setup.md の記述を「検証済み（日付）」に更新
- **手順が古い／動かない** → setup.md を修正。**動かない状態で配布し続けるのが最悪**
- **読み取り専用を機械的に確認できない** → setup.md に「Codex では能力の有無を機械的に検証できない」
  と明記し、**【A】常時適用型（学習用リポジトリを分ける運用）を推奨する**書き方に変える

**波及。** ここまでの実験結果はすべて Claude Code 上のもの。
**Codex を正式にサポートするなら、2経路の崩壊が Codex でも起きるかは別途測る必要がある**
（ハーネスは `claude -p` 前提なので、`codex exec` 相当に置き換える改修が要る）。
ただし優先度は低い。まず配置が成立するかが先。

**2026-09-08 の追記（配信機構の作業中に判明した分。実機検証ではなく文献確認のみ）。**

| 調べたこと | 結果 |
|---|---|
| Codex にライフサイクル hook はあるか | **ある。** `SessionStart` を含む12種。設定は `~/.codex/hooks.json` か `config.toml`、出力は `hookSpecificOutput.additionalContext` で **Claude Code とほぼ同形式**。スクリプト1本を両方で共有できる |
| `SessionStart` は実際に発火するか | **報告では発火しない。** [openai/codex#17532](https://github.com/openai/codex/issues/17532) が open のまま。`codex exec` では動くが `codex`（対話TUI）では動かないという検証が 0.128.0 / 0.132.0 で報告されている。OpenAI 側は `Stop` hook で再現しなかったと述べており、**状況は確定していない** |
| `model_instructions_file` / `sandbox_mode` vs `default_permissions` | **当時は未確認（404）だったが、2026-09-08 の準備セッションで解消。下記参照** |

**hook のドキュメントは移転している。** `developers.openai.com/codex/hooks` は
`learn.chatgpt.com/docs/hooks` へ 308 リダイレクトする。**予期したとおり、Codex の
ドキュメント構成自体が動いている。**

**この結果を受けて配信側でやったこと。** hook を主経路にせず、`mentor-update.py --check` を
手で回す経路を Codex では正規の手順とした。加えて **hook 自身が実行のたびに証跡を残し、
`--check` が「hook が一度も動いていません」を報告する**ようにした。
**動かないこと自体を検出できないと、「更新確認しているつもり」で古いまま使い続ける**ため。

**ついでにやると効率がよい。** 同じ機会に Copilot・Cursor・Gemini CLI の記述も確認する。
運用ガイドは「半年に一度、AIに公式ドキュメントで裏を取らせて更新する」と定めているが、
**その1回目がまだ実施されていない。**

**2026-09-08 の追記（実機検証の準備セッション）。** 実機での配置作業そのもの（上記1.）は
「setup.md の記述だけを見て、記憶や推測で補わない」ことが検証の眼目なので、ここでは代わりに
上記2.（文献確認）と、実機作業を妨げそうな環境要因の下調べだけを行った。

**文献確認の結果（`developers.openai.com/codex/config-reference` → `learn.chatgpt.com/docs/config-file/*` に
恒久リダイレクト。前回の404は解消していた）。**

| キー | 現行性 |
|---|---|
| `model_instructions_file` | **現行。** 「`AGENTS.md` の代わりに組み込み指示を置換する」という setup.md の説明と一致 |
| `sandbox_mode`（`read-only` / `workspace-write` / `danger-full-access`） | **現行。** 旧方式 |
| `default_permissions`（`:read-only` / `:workspace` / `:danger-full-access`） | **現行。** 新方式。公式が `sandbox_mode` からの移行先として案内している |
| 両者の併記 | **setup.md の書き方は正しかった。** 公式ドキュメントが明記：「`default_permissions` は `sandbox_mode` / `[sandbox_workspace_write]` と併用不可」＝**どちらか一方を選ぶ、という設計上の二択であり、ドキュメントの不確かさではなかった** |
| プロファイルの実体 | **`~/.codex/<name>.config.toml` という別ファイルで確定。** `[profiles.<name>]` を `config.toml` 内に書く旧方式は **Codex 0.134.0 以降で廃止**（`--profile` が読まなくなった）。setup.md の `~/.codex/learn.config.toml` という書き方は新方式と一致 |

**このマシンの環境調査（実機検証の前提が崩れていないかの確認）。**

- **`codex` は PATH に無い。** かわりに `~/.codex` を丸ごと使う「OpenAI Codex」デスクトップアプリ
  （`C:\Users\stama\AppData\Local\OpenAI\Codex\`、ドキュメント／表計算／ブラウザ操作などのプラグインを
  同梱した別製品）が既に入っており、その内部に `codex.exe`（**バージョン 0.153.4**、0.134.0 より新しいので
  新方式のプロファイルで問題ない）が同梱されている
- **`~/.codex/config.toml` は素の状態ではない。** 実運用中の個人設定（モデル、プラグイン、
  信頼済みプロジェクト一覧）が既に入っている。setup.md の検証は「素の状態で入れる」前提なので、
  **このファイルに直接 `learn` プロファイルを試すのは避け、`CODEX_HOME` 環境変数で
  一時ディレクトリ（例: 空の `~/.codex-learn-test/` に `config.toml`＋`auth.json` だけ用意）を指す形で
  隔離してから検証するとよい。** 認証情報だけは既存の `auth.json` をコピーすれば流用できる
- **`--strict-config` フラグが機械的な裏取りに使える。** `config.toml` に未知のキーがあると
  エラーで落ちるので、`learn.config.toml` を置いたあと `codex --strict-config --profile learn --version`
  的な軽い呼び出しで**キー名が現行かどうかを自己申告に頼らず確認できる**
  （運用ガイドが繰り返す「自己申告では判定できない」の弱点を、ここだけは埋められる）
- **`codex doctor` サブコマンドがある。** インストール・設定・認証を診断する組み込みコマンド。
  配置直後の一次切り分けに使える

**実機検証の記録欄（やりながら埋める）。**

- [ ] 隔離した `CODEX_HOME` に `learn.config.toml`（setup.md の記述のみを見て作成）
- [ ] `codex --strict-config --profile learn` がキーエラーなく起動する
- [ ] ops-guide 確認1：短いコードで「いきなりレビュー」にならず、説明ゲートが先に来る
- [ ] ops-guide 確認2：ツール一覧に `Read`/`Grep` 等の読み取り系のみで `Edit`/`Write`/シェル実行が無い
- [ ] ops-guide 確認3：MCP 接続時、`create_`/`update_`/`merge_`/`push_`/`delete_`/`_write` 系ツールが無い（今回は MCP 未接続なら省略可）
- [ ] ops-guide 確認4：3〜4往復させても「代わりに実装しましょうか」が出ない
- [ ] 詰まった箇所・ドキュメントとの食い違い：

---

### v0.1.0 リリース（実施中）

配信機構（`mentor-update.py`、`check-sync.py`、リリースワークフロー）は揃ったが、
**Releases が空のままだと hook を入れた全員が毎セッション
「リリースがまだ1つも公開されていません」を見る**（失敗は毎回出す方針の帰結。手元で再現済み）。
タグを打つのが先、hook の配布はその後。

**1.0.0 ではなく 0.1.0 にした理由。** 中身は揃っているが、**Claude Code 以外での配置が
一度も実機で確認できていない**（上記「Codex CLI での配置が実際に成功するかの検証」）。
1.0 を名乗ると「対応エージェントはすべて検証済み」と読まれる。
未検証の配置手順を検証済みとして配るのは、この製品が最も嫌う
「失敗しても何のエラーも出ない」の側に立つことになる。
**0.x のあいだは、構成・手順・配置先の指定が変わりうると宣言しておく。**

出したら以下2件を確認すること（README 整備〈2026-09-08〉の残作業）。

- **README のダウンロード手順。** `learning-mentor-v0.1.0.zip` を落として展開する経路を
  まだ誰も通っていない
- **リリースワークフローの実機実行。** ローカルで同じ手順を再現し、
  zip が自己検査（`check-sync.py`）を通ることまでは確認済みだが、Actions 上では未実行

---

## 完了（記録）

### 導入時のユーザー操作を 13 → 5 に減らした（2026-09-08）

配信機構を通しで数えたら、導入に**13操作**あり、**うち4つが手による設定ファイル編集**だった
（settings.json への JSON マージ、コマンドのパス書き換え、Codex の config.toml、
`--register` の手打ち）。**そのすべてが「失敗しても何のエラーも出ない」クラス**で、
この製品が最も嫌う失敗の型が導入手順の後半に固まっていた。

| 変更 | 効果 |
|---|---|
| `--register` を **`--setup`** に拡張（受領書＋hook 設置＋動作確認の案内） | 手編集4つが0に。setup.md 手順7 の1行で AI が自動実行する |
| README 手順4（`--register` を頼む）を削除 | setup.md 手順7 と同じ指示が2箇所にあり、AI が済ませたことをもう一度頼んでいた |
| setup.md を「貼る」から**「読ませる」**へ（コピペは代替経路として残す） | 枠の一部だけコピーしてマーカーを落とす事故が構造的に消える |
| 更新を `--apply` 1本に | `--apply` は自分で最新を取りにいくので、`--check` の先行実行は元から不要だった |
| hook 通知文とすべての案内を**絶対パス**に（受領書に `script_path` を追加） | 「スクリプトをどこに置いたか」を思い出す作業が消える |

**hook の衝突方針は「見つけたら触らず報告」。** 既存の `SessionStart` があれば書き込まず、
足すべき JSON を表示して手を引く。Codex の `config.toml` も `[features]` が既にあれば触らない
（同じテーブルを2回書くと TOML 全体が読めなくなり、Codex の設定ごと壊れる）。
自動マージのほうが手数は減るが、設定ファイルは学習者のものであり、黙って組み替えると
組み替え損ねたことも伝わらない。**この製品が一貫して選んできた「静かに直すより、うるさく報告する」**
の側に倒した。

**途中で見つけたバグ（同時に修正）。** `learning-mentor-setup.md` の貼り付け枠が、
**手順7 のコードフェンスで閉じていた**（39行目で開いた ``` が 134行目で閉じる）。
GitHub 上で「枠の中を丸ごとコピー」すると、**【運用の選択】と役割定義の本文300行が枠の外**
にあり、コピーされない。手順7 の入れ子フェンスを字下げに変えて解消（枠は 39→482 行）。
`check-sync.py` は本文マーカー以降しか見ないため、**この壊れ方は検査を素通りしていた。**

**やらなかったこと。** マーカー走査による配置先の自動発見（受領書を失ったときの復旧用）。
走査対象リストは陳腐化し、外したときに**黙って見つからない**。いま消したのと同じ失敗の型を
別の場所に作ることになる。`--setup` が確実に走るなら、そもそも受領書は失われない。

### 配信の仕組みを入れた（2026-09-08、`dc7fb92` / `515ff11`）

初回は zip を渡し、以降は各自が最新版に追従できるようにした。

- **`mentor-update.py`。** 受領書（どこに何を置いたか）の記録・新版の確認・貼り直し。
  配置先は `setup.md` がAIに判断させる設計なので、**記録しておかないと更新側から探し当てられない**
- **配置済みの本文をマーカーで囲む。** その内側だけを差し替えるので、`CLAUDE.md` に
  埋め込んだ場合でも学習者自身の記述を消さない。マーカーは配布物にだけ入り、
  リポジトリ側と `fixture` には入れていない（**被験体を1バイトも変えないため**）
- **`SessionStart` hook で検知、適用は人。** メンターは読み取り専用なので自分を書き換えられず、
  無断で更新すると学習者に知らせないまま挙動が変わる。**気づくのは自動、書き換えるのは人**
- **リリースは関門2つ。** タグと `VERSION` の一致、`check-sync.py` の通過。
  ずれたまま配ると受け取った側は何のエラーも見ずに別物を使うことになる

### README に導入方法と更新方法を書く（2026-09-08）

README を入口として書き直した（`515ff11`）。概要・できること・使い方・
導入方法（5手順）・更新方法・つまずいたときの対応表・ファイル一覧。
v1.0.0 未公開である旨は冒頭に注記のみ。

**計画から変えた点が2つある。** どちらも意図的なので記録しておく。

| 計画 | 実際 | 理由 |
|---|---|---|
| 対応エージェントごとの配置手順を **README に要約**して載せる | 要約せず、`learning-mentor-setup.md` を貼るよう案内するだけにした | ツールごとの機能名・パス・起動コマンドは**放置すると確実に古くなる**（運用ガイドが名指しで警告している箇所）。README に写すと、更新のたびに2箇所ずれる。`check-sync.py` まで書いて避けてきた事態を、手作業で再生産することになる |
| `check-sync.py` の役割・コマンドを明記する | 「開発用」の1行のみ | 利用者が実行するスクリプトではない。配布物の整合性を守るのは配る側の仕事で、リリースワークフローの関門に組み込んである（`dc7fb92`）。利用者向け README に置くと、実行すべきものだと誤解される |

**唯一 README 本文に実物を置いたもの。** 「使えるツールの名前を全部挙げてください」で
編集ツールが無いことを確認する手順。**配置に失敗してもエラーが出ない唯一の防御線**であり、
リンク先に畳むと確実に飛ばされる。飛ばされて困る度合いが、重複のコストを上回ると判断した。

残っている検証項目は「未完了 → v1.0.0 リリース」に記載。
