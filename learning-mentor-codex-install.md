# Codex を無料で用意する（Windows）

**読者：** AI コーディングツールをまだ何も入れていない人。**お金をかけずに**学習メンターを使い始めたい人。

すでに Claude Code や Codex が動いている人は、このファイルは要りません。→ **[README.md](README.md)** へ。

このファイルが案内するのは、**Codex を入れて、サインインして、動いていることを確かめる**までです。学習メンターそのものの導入は [README.md](README.md) が持ちます。最後に橋渡しします。

---

## 先に：無料でどこまでできるか

**Codex は ChatGPT の無料プランに含まれています。** クレジットカードは要りません（[公式ヘルプ](https://help.openai.com/en/articles/11369540-using-codex-with-your-chatgpt-plan)）。

ただし、無料プランには次の制限があります。

| | 無料プラン |
|---|---|
| 手元のPCでの作業（このファイルで使うもの） | **できる** |
| 使用量 | 全プランで**いちばん少ない**。上限に当たると、しばらく待つことになります |
| クラウド側の機能（自動コードレビュー等） | 使えません |

**上限の具体的な数値は、ここには書きません。** 公開されていないうえ、変わるからです。最新は上の公式ヘルプを見てください。

**無料枠で学習会を通せるかは、こちらでは測っていません。** 足りなくなったときの選択肢は最後の節にまとめました。

---

## 用意するもの

- Windows のPC（この手順は Windows 向けです）
- **ChatGPT のアカウント**（無料で作れます）
- **Visual Studio Code**（未導入なら [公式](https://code.visualstudio.com/) から）

---

## ① VS Code 拡張を入れる

1. VS Code を開き、左の**拡張機能**（四角が4つのアイコン / `Ctrl+Shift+X`）を開く
2. **`ChatGPT`** で検索し、**発行元が OpenAI** のものを選ぶ（拡張の ID は `openai.chatgpt`）
3. **インストール**を押す

入ったら、左のサイドバーに Codex のアイコンが増えます。**見当たらない場合**は `Ctrl+Shift+P` でコマンドパレットを開き、`Codex: Open Codex Sidebar` を実行してください。

サイドバーの案内に従って、**ChatGPT アカウントでサインイン**します。

> **「プランのアップグレードが必要」と言われたら、いったん拡張は置いて②へ進んでください。** 無料枠の扱いは変わりやすいところです。CLI 側でサインインできることのほうが、この先では重要です。

---

## ② CLI を入れる

**拡張だけでは足りません。** 学習メンターは、配置が本当に効いているかの確認と、新版のお知らせに **CLI のコマンドを使います**（[README.md](README.md) の「動作確認」と「更新のしくみ」）。

PowerShell を開いて、次を実行します。

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://chatgpt.com/codex/install.ps1 | iex"
```

Node.js が入っている人は、こちらでも構いません。

```powershell
npm install -g @openai/codex
```

入ったか確認します。

```powershell
codex --version
```

`codex-cli 0.1XX.X` のようにバージョンが出れば成功です。

### ★ ここで詰まる人がいます：`codex` が見つからない

```
codex : 用語 'codex' は、コマンドレット、関数、スクリプト ファイル、または
実行可能プログラムの名前として認識されません。
```

原因は、だいたい次の2つです。

| 原因 | どうなっているか | 対処 |
|---|---|---|
| **PowerShell を開く前に入れた** | インストール後に PATH が更新されたが、開いたままの窓には反映されない | **PowerShell を閉じて開き直す** |
| **拡張やデスクトップアプリだけ入れた** | `codex.exe` は `C:\Users\<あなた>\AppData\Local\OpenAI\Codex\bin\...` の中にあるが、**PATH には登録されていない** | 上のインストールコマンドで CLI を別途入れる |

**2つめは分かりにくい失敗です。** Codex 自体は画面上で動いているので入っていないようには見えず、それでいて `codex` コマンドだけが無い、という状態になります。

---

## ③ サインインして、動くことを確かめる

### サインインする

```powershell
codex
```

初回は選択肢が出るので、**`Sign in with ChatGPT`** を選びます。ブラウザが開くので、ChatGPT アカウントで許可してください。終わったら `Ctrl+C` で一度抜けて構いません。

### 無料枠を使わずにできる確認

```powershell
codex debug prompt-input "hello"
```

**JSON がどっと出れば成功です。** これは「モデルに渡る内容」を表示するだけのコマンドで、**モデルを呼ばないので無料枠を消費しません。** 何度でも試せます。

このあと学習メンターを配置したあとにも、**設定が効いているかを無料で確かめる**のに同じコマンドを使います。

### 1回だけ、実際に話してみる

```powershell
codex "こんにちは。いま動いていますか？"
```

返事が返ってくれば、ここまでは完了です。**ここで初めて無料枠を1回分使います。**

---

## ④ 学習メンターの導入へ

ここから先は **[README.md](README.md) の「導入方法」** に進んでください。やることは3つです。

1. [Releases](https://github.com/winc1980/learning-mentor/releases) から zip を落として展開する
2. 展開したフォルダで Codex に配置を頼む（`learning-mentor-setup.md` を読んで、運用A で配置してください）
3. **動作確認する**（ここを飛ばさない）

**[Bun](https://bun.sh) が要ります。** `bun --version` が答えなければ、先に入れてください。

> **Codex で運用A（常時適用型）を選ぶ人へ。**
> **そのリポジトリを Codex に「信頼済み」として登録する必要があります。** 登録されていないと、置いた設定は**黙って無視されます。** しかもそのとき読み取り専用に見えるので、**成功と区別がつきません。**
> 確認のしかたは `learning-mentor-ops-guide.md` の「配置後の動作確認」にあります。配置を頼むときに、**このファイルも一緒に読ませてください。**

---

## 無料枠が足りなくなったら

上限に当たると、しばらく使えなくなります。**エラーメッセージにいつ回復するかが出ます。**

選べるのは次のどれかです。

- **時間を置く**（無料枠は一定時間で回復します）
- **ChatGPT の有料プランに上げる**
- **別のツールを使う**（学習メンターは Claude Code / Copilot / Cursor などでも配置できます。置き場所は AI 自身が調べます）

**どれを選んだか、そのときどう困ったかを教えてください。** 無料枠でこの学習会を通せるかは、まだ測れていません。

→ [フィードバックフォーム](https://docs.google.com/forms/d/e/1FAIpQLSeltEAaD7Q86DgPaqVQnhO84CIWZQl-eblp9zJ6hHeV5m66Lg/viewform)

---

## この手順は古くなります

**Codex の入れ方・コマンド名・無料枠の中身は、よく変わります。** 上の表や手順が実際と食い違ったら、そこは**このファイルのほうが間違っています。**

最新は公式を見てください。

- [Codex CLI のインストール](https://developers.openai.com/codex/cli)
- [Codex の IDE 拡張](https://developers.openai.com/codex/ide)
- [ChatGPT のプランと Codex](https://help.openai.com/en/articles/11369540-using-codex-with-your-chatgpt-plan)

食い違いを見つけたら、上のフィードバックフォームで教えてください。
