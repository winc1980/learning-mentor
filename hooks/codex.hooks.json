{
  "_comment": "Codex CLI 用。~/.codex/hooks.json に置く。リポジトリ直下の .codex/ ではなくユーザー直下に置くこと（理由は hooks/README.md）。command のパスは書き換える。",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "bun \"$HOME/learning-mentor/mentor-update.ts\" --hook",
            "statusMessage": "学習メンターの更新を確認しています",
            "additionalContextLimit": 2000
          }
        ]
      }
    ]
  }
}
