# 配置用プロンプト

環境ごとに設定ファイルの置き場所が違うので、**AI自身に調べさせて配置させる**方式にしています。
使い方は、下の枠の中を丸ごとコピーして、使っているAIに貼るだけです。

- `【運用の選択】` の行に **A** か **B** を書く
- 最後に `learning-mentor-prompt.md` の中身を丸ごと貼り付ける

---

## 貼り付けるもの（ここから）

```
これから「学習・解説メンター」という役割定義を、この環境に合わせて配置したいです。
あなたが環境を調べて、適切な置き場所を判断してください。

## 手順

### 1. 環境を調べる
- あなた自身がどのAIコーディングツール上で動いているかを踏まえてください
- このリポジトリに次のようなものがあるか確認してください：
  .claude/ .cursor/ .github/ .gemini/ .clinerules/ .windsurf/ .codex/
  CLAUDE.md AGENTS.md GEMINI.md .cursorrules など
- そのツールで「常時適用される指示ファイル」と「明示的に呼び出す単位
  （スラッシュコマンド／プロンプトファイル／サブエージェント等）」が
  それぞれどう定義されるかを確認してください
- 記憶があいまいな部分は推測で断定せず、公式ドキュメントを確認するか、
  私に質問してください。間違った場所に置くと黙って無視されるだけなので、
  確信が持てない場合は必ず確認してください

### 2. 運用に合わせて配置方法を決める
私が選んだ運用（下の【運用の選択】）に従ってください。

【A】常時適用型
  用途：学習用リポジトリ。このリポジトリでは常にメンターとして振る舞ってほしい
  - そのツールで「プロジェクト全体に常時適用される指示ファイル」に
    あたる場所へ配置してください
  - 加えて、そのツールに **ファイル編集・コマンド実行を禁止する設定** が
    あるなら、その設定も提案してください。
    指示文だけだと、会話が長くなったときに効かなくなることがあります。
    設定で物理的に落とせるならそちらのほうが確実です

【B】呼び出し型
  用途：通常の開発プロジェクト。普段は普通に開発し、必要なときだけ呼びたい
  - そのツールで「ユーザーが明示的に呼び出す単位」に配置してください
    （スラッシュコマンド、プロンプトファイル、サブエージェント、
     カスタムモードなど、そのツールで使える形式）
  - **読み取り専用のサブエージェント／別セッションとして分離できる仕組みが
    あるなら、それを最優先してください。** 編集ツールを持ったままの
    セッションに指示を注入するだけの形は、効きが弱くなります
  - 呼び出し名は `learn` にしてください

### 3. 提案してから書く
配置先のパス、ファイル名、行う変更の一覧を **先に提示** してください。
私が確認してから書き込んでください。いきなり書かないでください。

### 4. 判断がつかない場合
該当する方式が判断できない場合は、リポジトリ直下の `AGENTS.md` に置いてください。
複数のツールが読む共通形式なので、外れにくいです。
その場合、その旨と、あなたがどこまで確認できたかを教えてください。

### 5. 動作確認の方法を教えてください
配置が終わったら、「ちゃんと効いているかを確かめる方法」を1つ教えてください。
（例：どう話しかければメンターとして応答するはずか）

## 【運用の選択】
A / B  ← どちらかを残す

## 配置する内容
以下をそのまま配置してください。内容は変更しないでください。

--- ここから下が本文 ---

（ここに learning-mentor-prompt.md の中身を丸ごと貼り付ける）
```

## 貼り付けるもの（ここまで）

---

## 補足：環境ごとの目安

AIが判断に迷ったときの参考です。**この表は変わる可能性があるので、最終的にはAIに確認させてください。**

| ツール | 【A】常時適用 | 【B】呼び出し型 |
|---|---|---|
| Claude Code | `CLAUDE.md`（リポジトリ直下） | サブエージェント（`.claude/agents/`）が最有力。次点でスラッシュコマンド（`.claude/commands/`） |
| Codex CLI | `AGENTS.md` | プロンプトファイル |
| Cursor | プロジェクトルール（`.cursor/rules/`） | カスタムコマンド／カスタムモード |
| GitHub Copilot | `.github/copilot-instructions.md` | プロンプトファイル（`.github/prompts/`） |
| Gemini CLI | `GEMINI.md` | カスタムコマンド（`.gemini/commands/`） |
| その他 | `AGENTS.md` | ── |

### 【A】を使うときの注意

**指示文だけでは事故ります。** 会話が長くなるとルールの効きが弱くなり、うっかりファイルを書き換えることがあります。
ツール側に編集権限を落とす設定があるなら、必ず併用してください（Claude Code なら `.claude/settings.json` の `permissions.deny` で `Edit` / `Write` を拒否する、など）。
設定の書き方はバージョンで変わるので、これも配置プロンプトの中でAIに調べさせてください。

### 【B】を「サブエージェント優先」にしている理由

スラッシュコマンドは、**いま開いている開発セッションに指示を差し込むだけ**です。そのセッションは編集ツールを持ったままなので、「コードを書かない」という指示と、セッションが持っている能力が矛盾します。長い会話では能力のほうが勝ちがちです。
サブエージェントや別セッションとして切り出せば、**そもそも編集できない状態**を作れます。使える環境ではそちらを選んでください。
