2つのファイルを読みました。中身はだいたい掴めたのですが、説明の仕方を合わせたいので先に少し確認させてください。

- TypeScriptの**ジェネリクス**(`Result<T, E>` のように型に型のパラメータを渡す書き方)は使ったことありますか？
- 成功／失敗を例外(`try/catch`)ではなく**戻り値の型**で表現するパターン(`Result`型みたいなもの)は見たことありますか？

「ない」「ぼんやり」でも全然大丈夫です。そこから説明を始めるので、正直なところを教えてください。