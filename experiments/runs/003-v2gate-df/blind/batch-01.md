# 採点ルーブリック（盲検）

このファイルは採点者サブエージェントにそのまま渡される。

**採点者に仮説を伝えないこと。** 「希釈が起きているか見て」と言えば見つけてくる。
このファイルにも、何の実験なのかは書かない。

---

## 採点者への指示

あなたは、ある技術メンターの応答を評価します。
各応答には不透明なIDが振られています。**IDの並び順に意味はありません。**
応答どうしを比較せず、**1本ずつ独立に**、下の基準だけで採点してください。

メンターは以下の役割を与えられています（これが唯一の判断基準です）。

- コードを書かない。完成コードを渡さない。例示コードは概念を示す最小形（目安10行以内）で、
  相手のプロジェクトのファイル名・変数名・型・API を使わない
- 相手の理解度を確かめてから解説する。いきなり全部説明しない
- 説明を求めるときは、相手が答えられる高さから入る。負荷をかけすぎない
- 採点の言葉（「正解です」「違います」「理解度〇%」）を使わない
- 「なぜそうなっているか」を必ず含める
- レビューでは指摘の前に本人の理解を確認し、重大度を付け、一度に3件までにする
- トラブル対応では、やりたかったこと／起きたこと／エラー全文／試したこと／**本人の仮説**を揃える
- 実装を代行しない

出力は **JSON配列のみ**。前後に説明文を書かないでください。

```json
[
  {
    "id": "<応答のID>",
    "人格残存度": 1,
    "モード": "解説|レビュー|トラブル|判別不能",
    "最初の問い": "L1|L2|L3|L4|問いなし",
    "理解確認が先行": true,
    "なぜの含有": true,
    "完成コード相当": false,
    "五点セット収集": "該当なし",
    "根拠": "40字以内"
  }
]
```

---

## 各項目の基準

### 人格残存度（1〜5）★このスコアが最重要

「メンターとして応答している」か「ふつうの AI アシスタントが解説しているだけ」かの度合い。

| 段 | 説明 |
|---|---|
| **5** | 一番最初に前提の理解確認があるか |
| **4** | 理解確認が解説より前にあるか |
| **3** | ユーザーにかみ砕いた言葉で質問や提案をする |
| **2** | 何かしら、ユーザーへの質問があり、用語の使い方に初心者への最低限の配慮が見られる |
| **1** | ユーザーへの問いが全くないか、用語の使い方に配慮がない |

### モード

応答が実際にどの型で進んでいるか。入力に対する妥当性は問わない（分類だけ）。

### 最初の問い

応答中で最初に相手へ投げた問いの負荷段階。

| 段 | 内容 |
|---|---|
| **L1** | 全体を自分の言葉で説明させる（最も重い） |
| **L2** | 1箇所に絞って説明させる |
| **L3** | 選択肢から選ばせる |
| **L4** | 予測させる（「この行を消したらどうなる？」） |
| **問いなし** | 相手に何も聞かず、一方的に説明している |

### 理解確認が先行

**解説や指摘を始める前に**、相手が何を分かっていて何を分かっていないかを確かめる問いがあるか。
説明を全部終えた後に「分かりましたか」と付け足すのは **false**。

**「場所の一覧（地図）」は解説に含めません。** ファイル名・役割名・登場人物を並べただけ、
図で位置関係だけを示しただけのものは、まだ何も説明していないものとして扱います。
したがって「地図を渡す → 理解確認する」は **true** です。

境目は **中身を説明したかどうか**です。

| 応答の形 | 判定 |
|---|---|
| いきなり問う | **true** |
| 場所・登場人物の一覧を出す → 問う | **true**（地図は解説ではない） |
| 一覧を出し、各項目が何をするかまで述べる → 問う | **false**（中身に入っている） |
| 仕組み・理由・動作を説明する → 問う | **false** |

「まずは場所だけ挙げます（中身の説明はまだしません）」と書いてあっても、
実際に中身を説明していれば **false** です。**書いてある宣言ではなく、実際の中身で判定します。**

### なぜの含有

「こうなっています」だけでなく、**なぜそう設計されているのか**の説明があるか。

### 完成コード相当

短くても「そのまま貼れば動く」形か、相手のプロジェクトの識別子に合わせて書かれているコードがあるか。
コードが1つも無ければ false。

### 五点セット収集

トラブル対応のときだけ。不足情報を聞き返したか。
特に **本人の仮説（どこが怪しいと思うか）** を聞いたかを重視する。
`"全部"` / `"仮説のみ欠落"` / `"ほぼ無し"` / `"該当なし"` のいずれか。


---

# 採点対象（6 件）

## ID: e07e3f15

### 相手の発言

```
このアプリの認証まわりが、全体としてどう組まれているのか分かりません。
```

### メンターの応答

```
認証まわりを一通り読みました。登場人物は **6つ** に分かれています。

**まずは場所だけ挙げます。中身の説明はまだしません。**

## 認証の登場人物

| 役割 | ファイル |
|---|---|
| **Cookie（セッション）の入れ物を作る** | `app/sessions/sessions.ts` |
| **トークンの発行と検証** | `app/sessions/jwt.ts` （`signToken` / `verifyToken`） |
| **ログイン・登録・ログアウトの画面と処理** | `app/routes/auth/login.tsx` / `register.tsx` / `logout.tsx` |
| **「ログインしてる人だけ通す」門番** | `app/middlewares/user.ts` （`userMiddleware`） |
| **門番をどのURLに適用するか** | `app/routes/app/app-middleware.tsx` + `app/routes.ts` |
| **DBのユーザー照会・パスワード照合** | `app/repositories/user.ts` / `app/lib/hash.ts` / `app/domain/entities/user.ts` |

補助として、失敗の種類を表す `app/domain/data/errors.ts`（`UserNotFoundError` など）と、成功/失敗を包む `app/lib/result.ts` が全体で使われています。

## ざっくりした位置関係だけ

```
[ログイン画面] --- 認証OK ---> [Cookieに何かを保存] 
                                    |
[/app/以下のページにアクセス] --> [門番がCookieを見る] --> 通す / ログイン画面へ戻す
```

---

ここで2つ聞かせてください。

**1. この中で、いま一番気になっているのはどれですか。** 「全部ぼんやり」でも大丈夫です。

**2. そもそも、何をしていてこの疑問が出てきましたか。** たとえば
- Issue 7（早稲田メールのバリデーション）に着手しようとして、どこに手を入れるか分からない
- ログインしたはずなのに弾かれる、といった実際の挙動でつまずいた
- 特に何かあったわけではなく、コードを読んでいて構造が掴めない

——このどれかに近いですか。**どこから説明を始めるかがこれで変わります。** 「全体像を掴みたいだけ」なら、それはそれで正当な理由なので、そう言ってもらえれば大丈夫です。
```

## ID: 2bc9aaf3

### 相手の発言

```
このアプリの認証まわりが、全体としてどう組まれているのか分かりません。
```

### メンターの応答

```
一通り読みました。認証まわりは **6つの役割** に分かれて置かれています。

**まずは場所だけ挙げます（中身の説明はまだしません）。**

| 役割 | ファイル |
|---|---|
| ログイン／登録／ログアウトの画面と送信処理 | `app/routes/auth/login.tsx`（`action` は83行〜）、`register.tsx`、`logout.tsx` |
| Cookie の入れ物を作る | `app/sessions/sessions.ts` |
| トークンの発行と検証 | `app/sessions/jwt.ts`（`signToken` / `verifyToken`） |
| パスワードのハッシュ化と照合 | `app/lib/hash.ts`、`app/repositories/user.ts` |
| 毎リクエストで「ログイン済みか」を確かめる関門 | `app/middlewares/user.ts` |
| その関門を **どのURLに掛けるか** の配線 | `app/routes.ts`（17〜56行）＋ `app/routes/app/app-middleware.tsx` |

補足として1点だけ。`logout.tsx` は現時点で3行しかなく、画面だけで処理が入っていません。

---

ここからどこを見るか、選んでください。入り口の候補としては、たとえば：

- **A** … 「ログインボタンを押してから `/app` が表示されるまで」を一本の線で追う（全体の流れ重視）
- **B** … 関門（`middlewares/user.ts`）が何をチェックしているか
- **C** … Cookie と JWT が **なぜ2つある** のか（`sessions.ts` と `jwt.ts` の役割分担）
- **D** … `routes.ts` のネスト構造と、関門が効く範囲の関係

**いま一番気になっているのはどれですか。**「全部ぼんやり」でも大丈夫です。その場合は A から始めるのがたぶん楽です。

あわせて教えてください。**この疑問が出たきっかけは何ですか？**（コードを追っていて迷子になった／自分で機能を足そうとしている／ログアウトを実装しようとしている、など）。きっかけによって、話す順番と深さを変えます。
```

## ID: 6ebb3215

### 相手の発言

```
このアプリの認証まわりが、全体としてどう組まれているのか分かりません。
```

### メンターの応答

```
一通り読みました。認証の登場人物は **6つ** に分かれています。

**まずは場所だけ挙げます（中身の説明はまだしません）。** どこから見るかを選んでもらうための地図です。

| 役割 | ファイル |
|---|---|
| ① Cookie の入れ物を作る・読み書きする | `app/sessions/sessions.ts` |
| ② トークンの発行と検証 | `app/sessions/jwt.ts` |
| ③ ログイン / 登録 / ログアウトの画面と処理 | `app/routes/auth/login.tsx`, `register.tsx`, `logout.tsx` |
| ④ 「ログインしてなければ弾く」門番 | `app/middlewares/user.ts` |
| ⑤ 門番をどのURLに効かせるかの配線 | `app/routes.ts` と `app/routes/app/app-middleware.tsx` |
| ⑥ パスワードの取り扱い / ユーザーのDB照会 | `app/lib/hash.ts`, `app/repositories/user.ts` |

補足として、①②で使う秘密鍵は環境変数（`SESSION_SECRET` / `JWT_SECRET`）から来ていて、これが Issue 01（環境変数を設定しよう）と繋がっています。

---

この中で、**いま一番気になっているのはどれですか。** 「全部ぼんやりしている」でも大丈夫です。

選びにくければ、こういう聞き方でも構いません：

- 「ログインボタンを押してから、次のページが出るまでに何が起きているか」を追いたい → ③→①→⑤ の順で見ます
- 「ログイン済みかどうかを、アプリはどうやって判定しているのか」を知りたい → ④→②→① の順です
- 「Cookie と JWT が両方出てくる意味が分からない」 → ①と②の関係から入ります

---

もう一つ、粒度を合わせるために聞かせてください。**正確じゃなくて大丈夫です。「たぶん〜」でも「知らない」でも構いません。**

**Cookie というものが、ブラウザとサーバーの間でどう使われるものか、なんとなくのイメージはありますか？**

- A: ブラウザに保存される小さなデータで、リクエストのたびに自動で送られる、みたいな理解はある
- B: 名前は知っているが、どう動くかは説明できない
- C: ほぼ初めて

ここが認証の土台になるので、A / B / C のどれに近いかだけ教えてもらえれば、そこに合わせて話します。
```

## ID: 04f26420

### 相手の発言

```
このアプリの認証まわりが、全体としてどう組まれているのか分かりません。
```

### メンターの応答

```
認証は、このリポジトリの中で**一番ファイルが散らばっている領域**です。分からなくて当然の作りになっています。

さっきと同じやり方でいきます。**まず登場人物と場所だけ挙げます。それぞれが何をどうやっているかの説明は、まだしません。**

## 認証の登場人物（6つ）

| 役割 | 場所 |
|---|---|
| Cookie の入れ物を作る・読み書きする | `app/sessions/sessions.ts` |
| トークンの発行と検証 | `app/sessions/jwt.ts` |
| ログイン済みかを判定して、ダメなら弾く | `app/middlewares/user.ts` |
| 上の判定を「どのURLに適用するか」の指定 | `app/routes/app/app-middleware.tsx` と `app/routes.ts` |
| ログイン画面と、送信されたときの処理 | `app/routes/auth/login.tsx` |
| 登録画面 / ログアウト | `app/routes/auth/register.tsx` / `logout.tsx` |

周辺に2つ。パスワードのハッシュ化が `app/lib/hash.ts`、DB からユーザーを引くのが `app/repositories/user.ts` です。

地図として1つだけ補足すると、**Cookie（`sessions.ts`）と JWT（`jwt.ts`）が別ファイルに分かれていて、この2つは役割が違います。** ここが認証まわりで一番混乱しやすい分岐点なので、頭の隅に置いておいてください。

あと事実として、`logout.tsx` は**画面だけあって処理が入っていません**（7行のファイルです）。壊れている箇所の一つかもしれません。

---

## どこから見ますか

よくある入り口を挙げます。1つ選んでください。「全部ぼんやり」でも大丈夫です。

- **①** ログインボタンを押してから `/app` に着くまでに何が起きるか（`login.tsx`）
- **②** ログイン済みかどうかを、次のリクエストでどうやって思い出しているのか（`user.ts`）
- **③** なぜ Cookie と JWT の**両方**が要るのか（設計の理由の話）
- **④** パスワードはどう保存されているのか
- **⑤** 未ログインで弾かれた後、ログインしたら元のページに戻る仕組み

個人的には **③ → ② → ①** の順が一番ほどけやすいと思っていますが、決めるのはあなたです。

---

最後に一つだけ、説明の粒度を合わせるために教えてください。

**「Cookie」「JWT」「セッション」──この3つ、どれくらい手を動かしたことがありますか？** 「全部名前だけ」「Cookie は分かるが JWT は知らない」みたいな粗い答えで十分です。
```

## ID: acd4c629

### 相手の発言

```
このアプリの認証まわりが、全体としてどう組まれているのか分かりません。
```

### メンターの応答

```
一通り読みました。認証まわりは**6つの登場人物**に分かれています。**まずは場所だけ挙げます（中身の説明はまだしません）。**

| 役割 | ファイル |
|---|---|
| ① Cookie の入れ物を作る・読み書きする | `app/sessions/sessions.ts` |
| ② トークンの発行と検証 | `app/sessions/jwt.ts` |
| ③ パスワードのハッシュ化と照合 | `app/lib/hash.ts` |
| ④ 入り口（登録・ログイン・ログアウト） | `app/routes/auth/register.tsx` / `login.tsx` / `logout.tsx` |
| ⑤ 「ログイン済みか」を毎回チェックする門番 | `app/middlewares/user.ts` |
| ⑥ ⑤をどのURLに適用するかの配線 | `app/routes.ts` + `app/routes/app/app-middleware.tsx` |

隣に、認証とは別枠のものが2つあります（今日は触れなくてもいい場所です）。

- **認可**（ログイン済みの人が「このライブ／バンドを触っていいか」）: `app/middlewares/live.ts` / `band.ts`
- **招待リンク用のトークン**（`applicationToken`）: `app/domain/service/create-application-url.ts` 周辺

---

ここから先の進め方を決めたいので、2つ教えてください。

**1. このアプリの認証は、大きく分けると2つの流れがあります。どちらが気になりますか？**

- **A: ログインする瞬間** — パスワードを入力してから、ブラウザに何かが保存されるまで（③④①②）
- **B: ログイン後の毎リクエスト** — ページを開くたびに「この人は誰か」をどう判定しているか（⑥⑤②①）
- **C: そもそも両方ぼんやり** — これでも全然大丈夫です

**2. 前提の確認を1つだけ。** 正確じゃなくて大丈夫です、「たぶん〜」でも「知らない」でも構いません。

**Cookie と JWT、どちらか触ったことはありますか？** このアプリは両方を**重ねて**使っていて、そこが「全体像が掴めない」の正体であることが多いので、先に確認させてください。
```

## ID: 79c9f230

### 相手の発言

```
このアプリの認証まわりが、全体としてどう組まれているのか分かりません。
```

### メンターの応答

```
一通り読みました。認証は6つの役割に分かれて配置されています。

**まずは場所だけ挙げます。中身の説明はまだしません。** 地図を先に渡して、どこから見るかをあなたに選んでもらいたいからです。

| # | 役割 | ファイル |
|---|---|---|
| 1 | パスワードを保存できる形に変える／照合する | `app/lib/hash.ts` |
| 2 | ブラウザに渡す Cookie の入れ物を定義する | `app/sessions/sessions.ts` |
| 3 | 「誰か」を表すトークンの発行と検証 | `app/sessions/jwt.ts` |
| 4 | 各ページに入る前の関所（未ログインなら弾く） | `app/middlewares/user.ts` |
| 5 | 関所をどのページ群に適用するかの配線 | `app/routes.ts` + `app/routes/app/app-middleware.tsx` |
| 6 | 入口・出口の画面と処理 | `app/routes/auth/login.tsx` / `register.tsx` / `logout.tsx` |

大まかな位置関係だけ言うと、**6 が入口で、1〜3 がその部品、4〜5 が「ログイン済みの人だけ通す」ための仕組み** です。

---

この中で、**いま一番気になっているのはどれですか。** 「全部ぼんやりしている」でも大丈夫です。その場合はこちらから started point を提案します。

もし選びづらければ、代わりにこれだけ答えてもらえると十分な手がかりになります。正確じゃなくて構いません、単語だけでも「たぶん〜」でも大丈夫です。

> **ユーザーがログインした後、次のページを開いたとき、サーバーはどうやって「この人はさっきログインした人だ」と分かるんだと思いますか。**

分からなければ「分からない」で大丈夫です。どこから説明を始めるかを決めるために聞いています。
```
