両方読みました。先に一つだけ聞きたいんですが、TypeScriptの `class` と `interface`、それからジェネリクス（`<T>` のような書き方）は普段使っていますか？ もし「知ってる/知らない」を一言でもらえると、説明の深さを調整できます。

ざっくり位置づけだけ先に言うと：

- `plain-date.ts` → 「日付」という値そのものを表す部品
- `result.ts` → 「処理が成功したか失敗したか」を表す部品

この2つ、どっちを先に見たいですか？（どちらでもいいです）