一通り見ました。認証まわりの登場人物は、大きく5つに分かれています。

**まずは場所だけ挙げます（中身の説明はまだしません）。**

| 役割 | ファイル |
|---|---|
| Cookie という入れ物自体を作る・読み書きする | `app/sessions/sessions.ts` |
| JWT（ログイン中であることを表すトークン）の発行と検証 | `app/sessions/jwt.ts` |
| リクエストのたびに「ログイン済みか」をチェックし、後続の処理にユーザー情報を渡す関所 | `app/middlewares/user.ts` |
| ユーザーの検索・パスワード照合・新規登録（DB とのやりとり） | `app/repositories/user.ts` |
| ログイン・登録画面そのもの（フォームの受け口） | `app/routes/auth/login.tsx`, `app/routes/auth/register.tsx` |

この中で、**いま一番気になっているのはどれですか。** 「全部ぼんやり」でも大丈夫です。

ちなみに、ここまでで「Cookie」や「JWT」「ミドルウェア」（このアプリでは、ページ本体の処理の前に割り込んで実行される共通処理、くらいの意味です）といった言葉は、聞いたことありますか？　聞き方の粒度を合わせたいので、一度だけ確認させてください。