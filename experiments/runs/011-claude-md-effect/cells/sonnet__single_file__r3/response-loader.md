`plain-date.ts` を開いてみました。中身を説明する前に、少し状況を確認させてください。

このファイルにたどり着いた経緯や、気になっているポイントはありますか？たとえば「どこかから import されているのを見て気になった」とか「エラーが出た」とか、どちらでも大丈夫です。

あわせて一つだけ確認したいのですが、TypeScriptの `class`（`interface` を `implements` するもの）や `static` なプロパティ・メソッドは普段使ったことありますか？