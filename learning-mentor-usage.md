# 使い分けとカスタマイズ

**読者：** 学習メンターを一度使ってみて、「自分に合わせて調整したい」と思った人。

はじめての人は [README.md](README.md) を先に読んでください。使い方はそちらで足ります。
このファイルは、**選べるところと、その選び方**だけを集めた場所です。

---

## 読むときの約束

このファイルの各項目には、根拠の強さを付けてあります。**強さの表示がないものは書きません。**

| 表示 | 意味 |
|---|---|
| **[測定済]** | 実験で確かめた。run 番号を添える |
| **[観察]** | 使っていて気づいたが、測ってはいない |
| **[未測定]** | まだ何も分かっていない。**選択肢だけ示して、推奨は出さない** |

**[未測定] のところで迷ったら、変えないでください。** 既定のままが一番情報があります。

---

## モデルの選び方

**[未測定]**

現状こうです。

- 実験（run 002〜006、102 セル）は**すべて opus** で回しています
- sonnet を含む条件は run 001（n=1）だけで、**そこから何かを結論できる量ではありません**
- したがって「解説なら軽いモデル、レビューなら強いモデル」といった使い分けは、
  **もっともらしいだけで、根拠がありません**

**いま言えること。** モデルを変えると、規定の守られ方も変わりうる、ということだけです。
run 004 で分かったのは「直前のターンで詳細な解説を済ませていると、理解確認を飛ばして
一方的に解説し始める」という崩れ方があることで、この崩れやすさがモデルごとに違うのかは
測っていません。

**どうすればいいか。** 既定（各ツールの標準モデル）のまま使ってください。
変えて試す場合は、[動作確認](README.md#3-動作確認するここを飛ばさないでください)を
やり直したうえで、
[フィードバック](https://github.com/winc1980/learning-mentor/issues/new?template=feedback.md)
で「モデルを変えたら、こう変わった」を報告してもらえると、それが測る対象になります。

---

## 版の選び方（現行版 / worked example 版）

**[未測定]**

役割定義には、比較検討中の版があります。**まだ配っていません**（実験ハーネスの中だけ）。

| 版 | 何が違うか |
|---|---|
| **現行版** | 完成コードを見せない。ヒントを段階的に具体化していく |
| **worked example 版**（`wex-fine`） | 完成コードを見せてよい。ただし**一度に 1 ブロックだけ**で、見せるたびに自己説明を挟む |

**なぜ 2 つ目があるか。** 学習会で「そもそも正解のコードを見せて解説してもらえた方が良い」
という声が出たためです。この声自体は**どちらが学習に効くかの証拠ではありません**が、
前提知識の乏しい学習者には完成例を見せたほうが効率が高い、という所見（worked example 効果）
はあります。現行の配布物は「コードなし＋理解あり」と「コードあり＋理解なし（＝普通の AI）」
の二択しかなく、**「コードあり＋理解あり」が存在しない**。そこを埋める候補です。

**比較はこれからです。** 結果が出るまで、選択肢としては提供しません。

---

## 運用A / 運用B の選び方

**[観察]**

README の[導入方法](README.md#2-配置する)にある表のとおりです。ここでは重複させません。

**迷ったら B。** A（常時適用型）はそのリポジトリでの開発ができなくなるので、
**学習専用のリポジトリを作った場合だけ**にしてください。

---

## GitHub MCP を繋ぐかどうか

**[観察]**

繋ぐと、メンターが issue や PR を読めるようになります。「この issue の何が分からないか」
を相談できるようになる一方、**能力を足す変更**なので、読み取り専用に絞る必要があります。

手順と、書き込み系が本当に無いことの確認方法は
[learning-mentor-setup.md](learning-mentor-setup.md) の「補助：GitHub MCP を読み取り専用で繋ぐ」。

---

## ここに書き足すとき

**このファイルは、実験が進むたびに増えていく受け皿です。** 書き足すときは：

1. **根拠の強さ（[測定済] / [観察] / [未測定]）を必ず付ける**
2. [測定済] には run 番号を添える
3. **[未測定] の項目に推奨を書かない。** 選択肢と「いま言えること」までにとどめる

もっともらしい推奨は、測る動機を消してしまいます。
（この規律の理由：[docs/concept.md](docs/concept.md)、[CLAUDE.md](CLAUDE.md)）
