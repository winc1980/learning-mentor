# CLAUDE.md

このリポジトリで作業する人・AI 向けの規約。**利用者向けの文書ではない**（利用者は
[README.md](README.md) を読む）。

企画としての意図・やらないこと・成功の判定は [docs/concept.md](docs/concept.md)。

---

## このリポジトリは何か

AI に「学習と解説だけ」をさせる役割定義（プロンプト）と、その配布・更新の仕組み、
そして**規定が守られるかを測る実験ハーネス**。プロダクトはコードではなく**文章**なので、
壊れてもコンパイルエラーは出ない。以下の規約はほぼ全部、そこから来ている。

---

## 触る前に知っておくこと

### 役割定義の本文は 1 箇所が正、コピーが 3 箇所ある

正は **`learning-mentor-prompt.md`**。本文はここから 3 箇所へコピーされている。

| コピー先 | 何のために |
|---|---|
| `learning-mentor-setup.md` の「--- ここから下が本文 ---」以降 | 配置用プロンプトに埋め込む用 |
| `.claude/agents/learn.md` | `claude --agent learn` の参照実装 |
| `../learning-mentor-fixture/.claude/agents/learn.md` | 実験ハーネスが被験体として起動する実体（fixture はリポジトリ外・追跡外） |

**本体を直したら、3 つを貼り直して `bun check-sync.ts` が通るまでが 1 つの変更。**
ずれたまま配ると、受け取った側はエラーを何も見ずに別物のメンターを使うことになる。
リリースの CI もここで止まる。

### fixture は書き換えない。そしてリポジトリの中に置かない

被験体が読むリポジトリ。既定の場所は **`../learning-mentor-fixture`**（このリポジトリの外）。
**1 バイトでも変わると、それ以降の run は過去の run と比較できない。**
追跡外なので git は守ってくれない。`experiments/run.py` が各セルの前後で
`verify-fixture.py` を呼んで止める。作り直しの手順は
[experiments/README.md](experiments/README.md)。

**リポジトリの中（`experiments/fixture`）に置いてはいけない。** cwd の祖先にある
CLAUDE.md は被験体のプロジェクト指示として読み込まれるので、**このファイルが被験体に渡る。**
規定が守られるかを測る実験で、被験体に規定の解説を渡すことになる。

これは静かに壊れる。fixture の中身は無改変のままなので `verify-fixture.py` も
`check-sync.ts` も通る。008 では 40 セル全部が検査を通り、`repo_sweep` 水準がたまたま
このリポジトリの構成を説明し始めたことでようやく気づいた（issue #42）。
いまは `verify-fixture.py` が「その場所で拾われる CLAUDE.md が無いこと」も見る。

### `experiments/runs/` は消さない・書き換えない

実験データ本体。過去の応答と採点結果がそのまま入っている。再取得には実費がかかる。

### ルーブリックには版がある

版 1（run 001〜006）と版 2 では **L3/L4 の意味が逆**。集計時に版を混ぜると
静かに壊れる。読み替え規則は [experiments/rubric.md](experiments/rubric.md)。

### VERSION とタグは一致させる

`VERSION` とタグ（`vX.Y.Z`）が食い違うとリリースが止まる。配置先は本文中のマーカーで
自分のバージョンを名乗るため、ここがずれると「v1.2.0 と名乗る v1.1.0 の本文」が出回る。

---

## やらないこと

- **TODO ファイル・進捗メモ・作業まとめを作らない。**
  これからやること = **GitHub Issues**（期日があるものは Milestones）、
  完了したことと理由 = **コミットメッセージ**。
  `TODO.md` と `experiments/BACKLOG.md` は 2026-09-08 に廃止済み。復活させない。
- **`docs/` に進捗・残作業を書き足さない。** `docs/` は腐らない文書だけを置く場所。
  残作業を持つのは Issues と Milestones だけ。
- **役割定義の本文を新しい場所にコピーしない。** すでに 3 箇所ある。参照するときはリンクで。
- **このリポジトリの CLAUDE.md（このファイル）にメンター本文を書かない。**
  運用A は `CLAUDE.md` への埋め込みを許すが、**ここでやるとこのリポジトリ自体の開発が
  できなくなる**（メンターはコードを書かない）。
- **ルート直下に新しいファイルを足さない。** ルート直下 = 配布物。内部文書は `docs/` へ
  （下記「配布物の境界」）。
- **「動いた気がする」で完了にしない。** 確認手段が自己申告しかないなら、issue にそう書く。

---

## 配布物の境界

| 置き場所 | 中身 | zip に入るか |
|---|---|---|
| ルート直下 | 利用者に届く文書とスクリプト | **入る** |
| `docs/` | 企画意図など、リポジトリ内部の文書 | 入らない |
| `experiments/` | 実験ハーネスとデータ | 入らない |
| `.github/` | CI・issue テンプレート | 入らない |

zip に何を入れるかの正は `.github/workflows/release.yml` の「配布物を固める」節。
**ルート直下にファイルを足したら、ここも直す。**

---

## 文書の書き方

- **日本語。** 冒頭で読者を名指しする（「初学者向け」「運営向け」）。
- **静かに壊れる変更は、それが「どう見えないか」を書く。**
  このプロダクトが繰り返し踏んできた失敗の型。置き場所を間違えてもエラーは出ないし、
  本文がずれても動く。「どう気づくか」まで書いて初めて説明になる。
- **測っていないことを推奨として書かない。** 根拠があるものは run 番号を、
  無いものは「未測定」と明示する。推奨が集まるのは
  [docs/tuning-options.md](docs/tuning-options.md)（根拠の強さを付けて溜める場所）なので、
  そこを厳しく。**配布物の [learning-mentor-usage.md](learning-mentor-usage.md) には、
  配れる状態になった選択肢だけを移す。**
- **選好を効果の証拠として扱わない。** 「参加者が気に入った」は期待の表明であって、
  学習に効いた証拠ではない（[experiments/DECISIONS.md](experiments/DECISIONS.md)）。

---

## よく使うコマンド

```bash
bun check-sync.ts                    # 本文のコピー 3 箇所がずれていないか（変更のたび）
bun mentor-update.ts --check         # 配置先・hook・最新版の状態を見る（更新はしない）

python experiments/verify-fixture.py                       # fixture が汚れていないか
python experiments/run.py experiments/specs/<spec>.yaml     # 実験を回す
python experiments/blind.py make <run-id>                   # 盲検バッチを作る
python experiments/analyze.py <run-id>                      # 条件ごとに集計する
```

`python` が無い環境では `py -3`。

---

## コミットとブランチ

- `main` へ直接コミットしない。ブランチを切って PR で入れる。
- 並行作業は **git worktree** を使う（`../learning-mentor-<用途>`）。
  実験の run と文書の編集が同じ作業ツリーで混ざると、fixture の状態が読めなくなる。
- コミットメッセージは `feat:` / `fix:` / `docs:` / `chore:` + 日本語。
  **何をしたかではなく、なぜそうしたかを書く。** 進捗記録の代わりなので。

---

## issue を立てるとき

`.github/ISSUE_TEMPLATE/` の型を使う。「やること一行」の issue を作らないための枠なので、
**埋まらない欄があるなら、それ自体が「まだ着手できない」というサイン。**

| 型 | 何を書くか |
|---|---|
| `experiment.md` | ハーネスでセルを回す測定実験。**判定（どういう結果が出たら何を結論するか）を、回す前に書く** |
| `task.md` | 配布・配置・更新の仕組み、文書、ハーネスの改善 |
| `feedback.md` | 利用者から届いた声。**運営が起票する**（主力ルートは Google フォーム） |

実学習者での評価（アンケート・観察・LT の採点）は `task.md` を使い、`evaluation`
ラベルを付ける。**`experiment` と同じ扱いにしない。** 固定台本・AI 被験体で測った
結果と、実学習者から得た所見では、言えることの範囲が違う。

**測らないと決めたことは issue にしない。** [experiments/DECISIONS.md](experiments/DECISIONS.md)
に書く。issue は閉じると忘れられるが、あれは将来の提案を却下するための根拠なので、
閉じてはいけない。
